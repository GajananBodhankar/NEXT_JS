// here, middleware is one more feature of NextJS
// Same as middleware of express js
// The middleware data in next js is seen in
// the terminal where the next js is running not in the console.log
// of the DOM
export function middleware() {
  console.log("in middleware");
}
